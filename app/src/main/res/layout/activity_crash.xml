<?xml version="1.0" encoding="utf-8"?>
<!-- شاشة عطل غير متوقع: أيقونة تنبيه + شرح مبسّط، ثم بطاقة تعرض نص الخطأ
     الكامل (قابل للتحديد/النسخ) لمساعدة تشخيصه، وزرا نسخ/إعادة تشغيل. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_app_gradient"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingTop="48dp"
    android:paddingEnd="20dp"
    android:paddingBottom="20dp">

    <ImageView
        android:layout_width="46dp"
        android:layout_height="46dp"
        android:layout_gravity="center_horizontal"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_alert"
        android:tint="@color/accent_red" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:fontFamily="serif"
        android:gravity="center"
        android:text="حدث خطأ غير متوقع"
        android:textColor="@color/text_primary"
        android:textDirection="rtl"
        android:textSize="21sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/crash_subtitle"
        style="@style/Phizyo.Body.Secondary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="انسخ التفاصيل بالأسفل وأرسلها للمساعدة في إصلاح المشكلة." />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="18dp"
        android:layout_weight="1"
        android:background="@drawable/bg_input_field"
        android:padding="14dp"
        android:scrollbars="vertical">

        <TextView
            android:id="@+id/crash_trace"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textColor="@color/text_secondary"
            android:textDirection="ltr"
            android:textIsSelectable="true"
            android:textSize="11.5sp" />
    </ScrollView>

    <TextView
        android:id="@+id/crash_copy"
        style="@style/Phizyo.ActionText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_glass_chip"
        android:drawableStart="@drawable/ic_share_out"
        android:padding="12dp"
        android:text="نسخ تفاصيل الخطأ" />

    <TextView
        android:id="@+id/crash_restart"
        style="@style/Phizyo.Button.Primary"
        android:layout_marginTop="12dp"
        android:text="إعادة تشغيل التطبيق" />
</LinearLayout>
